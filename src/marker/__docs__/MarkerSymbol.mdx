---
name: Marker Symbol
---

import { Playground, PropsTable } from 'docz'

import { Marker } from '../Marker'
import { MarkerSymbol } from '../MarkerSymbol'
import { GoogleMap } from '../../google-map/GoogleMap'
import { path } from './marker-symbol.json'

# Marker Symbol

Defines icon options of `Marker` component.

**Google Maps Docs:**
* [google.maps.Symbol](https://developers.google.com/maps/documentation/javascript/reference#Symbol)

<Playground>
  <GoogleMap
    zoom={8}
    maps={google.maps}
    style={{ height: '320px' }}
    center={{ lat: 36.964, lng: -122.015 }}
  >
    <Marker
      position={{ lat: 36.964, lng: -122.015 }}
      icon={
        <MarkerSymbol
          path={path}
          scale={0.7}
          rotation={45}
          fillColor="#404040"
          fillOpacity={1}
          strokeColor="#ffffff"
          strokeWeight={0.1}
          strokeOpacity={1}
          anchor={{ x: 10, y: 25 }}
          labelOrigin={{ x: 32, y: 32 }}
        />
      }
    />
  </GoogleMap>
</Playground>

<PropsTable of={MarkerSymbol} />
